// check-dupes.cjs — DEPRECATED. Duplicate detection is now part of
// verify-data.cjs (checks [3] and [4]). For per-recipe coverage stats,
// run `node data-stats.cjs` instead.
console.log("This script has been merged into verify-data.cjs.");
console.log("Run `node verify-data.cjs` for full validation.");
console.log("Run `node data-stats.cjs` for per-recipe coverage stats.");
